import { createStore } from 'vuex'

export default createStore({
   state: {
      data: []
   },
   getters: {
   },
   mutations: {
      GET_DATA(state, respoonse) {
         
      }
   },
   actions: {
   },
   modules: {
   }
})
